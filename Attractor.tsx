export default class Attractor {
  p: any;
  alpha: any;
  beta: any;
  gamma: any;
  delta: any;

  constructor(p, alpha, beta, gamma, delta) {
    this.p = p;
    this.alpha = alpha;
    this.beta = beta;
    this.gamma = gamma;
    this.delta = delta;
  }
}
